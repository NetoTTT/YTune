{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray-popup",
  "description": "Capability for the tray popup window",
  "windows": ["tray-popup"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "core:window:allow-start-dragging"
  ]
}
